/** Prefix streamed by the API when an error occurs, so the client can style it. */
export const ERROR_SENTINEL = " BAKA_ERR ";

/** Prefix streamed at the very end carrying token-usage JSON: {"in":N,"out":N}. */
export const USAGE_SENTINEL = " BAKA_USAGE ";

export const APP_NAME = "Baka";
export const DEVELOPER = "Biplab18";
